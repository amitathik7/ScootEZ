import React from 'react';

export default function CurrentRentalsPage() {
    return(
        <div>
            Current rentals page - where all the current rents will populate
        </div>
    );
}